@model IEnumerable<aspBattleArena.Models.Boss>
@* *@
@* @{ *@
@*     ViewData["Title"] = "Index"; *@
@* } *@

<h1>Szefowie</h1>

@if (User.Identity.IsAuthenticated)
{
    <p>
        <a asp-action="Create">Stwórz nowego szefa</a>
    </p>
}
<table class="table">
    <thead>
    <tr>
        <th>
            Id
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstName)/ Imiê
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)/ Nazwisko
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Age)/ Wiek
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nationality)/ Narodowœæ
        </th>
        <th>
            Organizacja
            @* @Html.DisplayNameFor(model=>model.Organization) *@
        </th>

    </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BossId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Age)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nationality)
            </td>
            <td>
                @Html.DisplayFor(modelItem=>item.OrganizationName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BossId">Edytuj</a> |
                <a asp-action="Details" asp-route-id="@item.BossId">Detale</a> |
                <a asp-action="Delete" asp-route-id="@item.BossId">Usuñ</a>
            </td>
        </tr>
}
    </tbody>
</table>
