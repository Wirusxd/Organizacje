@model IEnumerable<aspBattleArena.Models.GangMember>

@{
    ViewData["Title"] = "Index";
}

<h1>Cz³onkowie gangu</h1>

@if (User.Identity.IsAuthenticated)
{
    <p>
        <a asp-action="Create">Stwórz nowego cz³onka</a>
    </p>
}
<table class="table">
    <thead>
        <tr>
            <th>
              Imiê
            </th>
            <th>
              Nazwisko
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nationality)/ Narodowoœæ
            </th>
            @if (User.Identity.IsAuthenticated)
            {
                <th>
                    @Html.DisplayNameFor(model => model.Strength)/ Si³a
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Endurance)/ Wytrzyma³oœæ
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Intelligence)/ Inteligencja
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Luck)/ Szczêœcie
                </th>
                <th>
                    Organizacja
                </th>
            }
            
           
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nationality)
            </td>
            @if (User.Identity.IsAuthenticated)
            {
                <td>
                    @Html.DisplayFor(modelItem => item.Strength)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Endurance)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Intelligence)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Luck)
                </td>
            }
            <td>
                @Html.DisplayFor(modelItem => item.Organization.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MemberId">Edytuj</a> |
                <a asp-action="Details" asp-route-id="@item.MemberId">Detale</a> |
                <a asp-action="Delete" asp-route-id="@item.MemberId">Usuñ</a>
            </td>
        </tr>
}
    </tbody>
</table>
